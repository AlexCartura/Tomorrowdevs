let width = parseFloat (prompt("insert the widht in meters", "0"));
let lenght = parseFloat (prompt("insert the lenght in meters", "0"));

let totalarea = width * lenght;
alert("The area is " + totalarea + " meters");